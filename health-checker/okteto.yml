name: health-checker
labels:
  app.kubernetes.io/component: health-checker
image: okteto/golang:1
command: bash
environment:
  - ADVANCED_HEALTH_ENABLED=true
sync:
  - .:/usr/src/app
persistentVolume:
  enabled: false
divert:
  ingress: catalog-chart
  service: health-checker
  port: 8080
